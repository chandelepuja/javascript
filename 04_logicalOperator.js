

var ssc = 75;
var hsc = 65;
//Only allow if ssc marks greater than equal 60 AND hsc greater than 65
var res = ssc>=60 && hsc>65 ? "Allow for interview" : "Don't allow Please";
console.log(`Security guard is saying : ${res}`);